/**
 * @file auth.types.ts
 * @description 인증 관련 타입 — 공유 패키지에서 재수출
 * @module types/auth
 */

export type {
  UserRole,
  UserProfile,
  LoginRequest,
  RegisterRequest,
  AuthState,
} from '@pompcore/types';
