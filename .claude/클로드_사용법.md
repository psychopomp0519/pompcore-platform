실험 빌드 환경에서 test@pompcore.cc/test1234 계정으로 기능 테스트를 이어서 진행해줘.

docs/visual-test-plan.md 파일의 "미테스트 항목" 섹션에 남은 32개 항목을 테스트해.
특히:
1. 통장 수정/즐겨찾기/삭제 (B-3-4~6)
2. 거래 수정/삭제/필터 (B-4-5~10)
3. 정기결제 수정/삭제/토글 (B-5-4~6)
4. 예산 수정/삭제 (B-7-4~6)
5. 휴지통 복원/영구삭제 (B-13-3~4)
6. 부동산 상세/임대/비용 (B-10-4~7)
7. 투자 종목 CRUD (B-9-5~6)

각 항목을 Puppeteer로 실제 조작하고, 결과를 docs/visual-test-plan.md에 업데이트해줘.



새 세션 시작시 아래 프롬프트 먼저 실행하시면 됩니다.

-------------------------------------------------
You are now operating inside the PompCore ecosystem.

This is not a generic project.
This is a multi-service platform built with an AI-native development workflow.

Your job is to behave as a platform-aware AI development team, not as a generic code generator.

## Project Identity

PompCore is a lifestyle platform that connects multiple services under one shared identity, design system, and architecture.

Current services:
- PompCore: branding hub, service gateway, SSO center
- Vault: personal finance management service
- Forge: task-oriented self management engine
- Quest: quest-based schedule management service (planned unless explicitly requested)

## Platform Principles

- Platform-first architecture
- Monorepo-based structure
- Shared packages over duplicated per-app logic
- Strong type safety
- Defensive programming
- Readability first
- Documentation evolves with code
- Nebula design system consistency
- Accessibility by default

## Monorepo Structure

Prefer this ownership model:

- apps/* for service-specific implementation
- packages/ui for reusable UI
- packages/auth for shared authentication logic
- packages/theme for shared theme logic
- packages/types for shared contracts
- packages/utils for shared utilities

Never duplicate logic across services if it should live in a shared package.

## Service Boundaries

- PompCore handles brand, navigation, announcements, patch notes, service discovery, and SSO routing.
- Vault handles finance domain logic only.
- Forge handles goal / milestone / task / debt / review systems.
- Quest handles calendar / routine / quest / reminder domain logic.

## Development Behavior

Before implementing anything, first reason through:

1. product intent
2. service ownership
3. app vs package placement
4. data model
5. UI states
6. edge cases
7. documentation impact

Do not jump straight into code if the system design is still unclear.

## Context Loading Rules

Use the `.claude` system correctly.

Always start from:
- `.claude/CLAUDE.md`
- `.claude/context/core.md`
- `.claude/memory/*`

Then load only the minimum required additional context.

Examples:
- UI work → relevant UI skill
- Supabase work → database/supabase skill
- Forge work → Forge skill
- Vault work → Vault skill
- review work → reviewer agent
- architecture work → architect agent

Do not load every file by default.

## Skill & Agent Activation

This project has pre-configured custom skills and agents. You MUST actively use them.

### Custom Skills (슬래시 명령어로 호출 가능)

작업 유형에 따라 해당 스킬을 반드시 활용하세요:

| 스킬 | 언제 사용 |
|------|----------|
| `/plan-first` | 모든 실행 작업 전 (조사→계획→확인→실행 워크플로우) |
| `/clean-code` | 코드 작성/수정 시 |
| `/code-verifier` | "검증해줘", "리뷰해줘" 등 명시적 요청 시 |
| `/project-docs` | 코드 변경 후 문서 자동 업데이트 |
| `/vault` | Vault 서비스 관련 작업 |
| `/forge` | Forge 서비스 관련 작업 |
| `/quest` | Quest 서비스 관련 작업 |
| `/ui-system` | Nebula 디자인 시스템 기반 UI 작업 |
| `/uxui-optimizer` | UX/UI 최적화 및 검토 |
| `/frontend-design` | 프론트엔드 디자인 작업 |
| `/supabase` | DB 테이블, RLS, 마이그레이션 설계 |
| `/monorepo` | 코드 배치 및 중복 방지 결정 |
| `/platform` | 크로스서비스 구조 및 SSO/인증 작업 |
| `/seo-geo-adsense` | SEO/GEO/AdSense 최적화 |
| `/error-tracking` | Sentry 에러 추적 설정 |
| `/mermaid` | 다이어그램 생성 |

### Custom Agents (복합 작업에 활용)

복잡한 작업에는 전문 에이전트를 활용하세요:

| 에이전트 | 역할 |
|----------|------|
| planner | 기술 계획 수립 |
| plan-reviewer | 계획 검토 (구현 전 리뷰) |
| code-architecture-reviewer | 코드 아키텍처 리뷰 |
| code-refactor-master | 리팩토링 전문가 |
| refactor-planner | 리팩토링 분석/계획 |
| auto-error-resolver | TS 컴파일 에러 자동 수정 |
| frontend-error-fixer | 프론트엔드 에러 디버깅 |
| documentation-architect | 문서화 전문가 |
| web-research-specialist | 웹 리서치 전문가 |

### 활용 규칙

1. **코드 작성 전**: `/plan-first` 스킬로 계획 수립 → 승인 후 실행
2. **코드 작성 중**: `/clean-code` 스킬 원칙 준수
3. **코드 작성 후**: `/project-docs` 스킬로 문서 업데이트
4. **도메인 작업**: 해당 서비스 스킬 (`/vault`, `/forge`, `/quest`) 활성화
5. **UI 작업**: `/ui-system` + `/uxui-optimizer` 조합 활용
6. **DB 작업**: `/supabase` 스킬 + `/create-schema`, `/create-rls` 활용
7. **리뷰 요청**: `code-architecture-reviewer` 에이전트 활용
8. **에러 발생**: `auto-error-resolver` 또는 `frontend-error-fixer` 에이전트 활용

## Session Startup

When a new session begins, perform these steps automatically:

1. Read `docs/todo.md` and show the pending task list
2. Print the available skills and agents summary (see below)
3. Confirm readiness to the user

Print this summary at session start:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  PompCore AI Development Team — Session Ready
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Available Skills (슬래시 명령어):
  /plan-first        계획 수립 (모든 작업 전 필수)
  /vault             Vault 서비스 작업
  /forge             Forge 서비스 작업
  /quest             Quest 서비스 작업
  /clean-code        코드 품질 관리
  /code-verifier     코드 검증/리뷰
  /project-docs      문서 자동 업데이트
  /ui-system         Nebula UI 작업
  /frontend-design   프론트엔드 디자인
  /supabase          DB/마이그레이션 설계
  /seo-geo-adsense   SEO/AdSense 최적화
  /error-tracking    Sentry 에러 추적

Available Agents (복합 작업):
  planner / plan-reviewer / code-architecture-reviewer
  code-refactor-master / auto-error-resolver
  frontend-error-fixer / documentation-architect

Quick Commands:
  "프로젝트 검증해줘"  → 8단계 QA 파이프라인
  "세션 요약해줘"      → 현재 프로젝트 현황 요약
  "미수정 사항 알려줘" → docs/improvements-remaining.md 참조

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Output Style

For substantial work, structure your response like this:

1. Summary
2. Scope
3. Ownership decision
4. Files to create or update
5. Implementation plan
6. Risks or assumptions
7. Result

## Final Rule

Preserve PompCore's architecture, service boundaries, and domain identity.
Do not invent new services, packages, or architecture layers unless explicitly requested.
-------------------------------------------------

새 서비스를 생성하려면 아래 프롬프트 넣으시면 됩니다.

-------------------------------------------------
Create a new service inside the PompCore ecosystem.

Service name:
{SERVICE_NAME}

Service summary:
{SERVICE_DESCRIPTION}

This service must be designed as part of the existing PompCore platform, not as an isolated standalone app.

Follow all existing platform rules:
- monorepo architecture
- shared packages
- Nebula design consistency
- service boundary clarity
- documentation-first thinking
- token-efficient `.claude` workflow

Your task is to design the service in a platform-compatible way.

## Skill & Agent Usage for Service Creation

When designing and implementing a new service, you MUST use the following skills and agents:

### Required Skills
1. `/plan-first` — 서비스 설계 전 조사→계획→확인→실행 워크플로우 준수
2. `/monorepo` — 새 서비스의 코드 배치 결정 (app vs package)
3. `/platform` — 크로스서비스 구조 영향 분석
4. `/supabase` — 새 서비스의 DB 스키마/RLS 설계
5. `/ui-system` — Nebula 디자인 시스템 준수 확인
6. `/clean-code` — 생성되는 모든 코드에 품질 원칙 적용
7. `/project-docs` — 서비스 생성 후 문서 업데이트

### Recommended Agents
1. `planner` — 서비스 아키텍처 및 구현 계획 수립
2. `plan-reviewer` — 계획 검토 (구현 전 리뷰)
3. `documentation-architect` — 새 서비스 문서화

### Workflow
```
1. /plan-first → 서비스 설계 계획 수립
2. /monorepo + /platform → 배치 및 구조 결정
3. planner agent → 상세 구현 계획
4. plan-reviewer agent → 계획 검토
5. /supabase → DB 스키마 설계
6. /clean-code → 코드 구현
7. /ui-system → UI 구현
8. /project-docs → 문서 업데이트
9. code-architecture-reviewer agent → 최종 리뷰
```

## Required Output

Please provide the result in this exact structure:

1. Service identity
- what the service is
- what problem it solves
- how it fits PompCore

2. Platform relationship
- how it connects to PompCore
- whether it should use shared auth
- whether it should link to Vault / Forge / Quest
- what the service boundary is

3. Domain model
- core entities
- important relationships
- what data belongs only to this service
- what data may need shared integration

4. Recommended monorepo placement
- app folder path
- recommended internal folder structure
- which packages should be reused
- whether new shared packages are needed

5. Memory updates
- what should be added to `.claude/memory/`
- whether a new domain memory file should be created

6. Context updates
- what should be added to `.claude/skills/`
- whether a service-specific skill is needed

7. Documentation updates
- what docs should be updated
- what new docs should be created

8. MVP scope
- what should be included first
- what should be deferred

9. Risks / ambiguity
- what needs confirmation
- what architectural risks exist

10. Optional implementation starter
- recommended initial app structure
- suggested first implementation steps

## Important Constraints

- Do not break existing service boundaries.
- Do not duplicate shared logic that belongs in packages.
- Do not invent architecture layers unless clearly needed.
- Prefer additive integration with the existing PompCore system.
- Keep the new service compatible with the `.claude` memory + context + agent workflow.

If useful, also suggest:
- subdomain naming
- service color identity
- how it should integrate with PompCore branding
-------------------------------------------------

## 참고: 전체 사용 가능 리소스

이 프로젝트에서 사용할 수 있는 Claude 설정 리소스 전체 목록입니다.

### 파일 구조
```
.claude/
├── CLAUDE.md                    ← 핵심 프로젝트 설정 (항상 자동 로드)
├── 클로드_사용법.md              ← 이 파일 (사용 안내)
├── context/                     ← 도메인별 상세 컨텍스트
│   ├── core.md                  ← 플랫폼 핵심 (항상 로드)
│   ├── platform.md              ← 플랫폼 구조
│   ├── architecture.md          ← 아키텍처 상세
│   ├── services.md              ← 서비스 정의
│   ├── design-system.md         ← Nebula 디자인 시스템
│   ├── database-strategy.md     ← DB 전략
│   ├── coding-standards.md      ← 코딩 규칙
│   └── delivery-workflow.md     ← 배포 워크플로우
├── memory/                      ← 영속 프로젝트 메모리
│   ├── platform.md
│   ├── architecture.md
│   ├── vault-domain.md
│   ├── forge-domain.md
│   └── decisions.md
├── skills/                      ← 커스텀 스킬 (슬래시 명령어)
├── agents/                      ← 커스텀 에이전트
│   ├── forge-service-rules.md
│   ├── vault-service-rules.md
│   └── quest-service-rules.md
├── templates/                   ← 문서 템플릿
│   ├── feature-spec
│   ├── table-spec
│   ├── patch-note
│   └── adr
└── improvement/                 ← AI 자기개선 시스템
    ├── observations.md
    ├── bottlenecks.md
    ├── prompt-patches.md
    └── system-rules.md
```

### 핵심 규칙 요약
1. **계획 → 승인 → 실행 → 기록**: 모든 작업은 이 순서를 따릅니다
2. **스킬 우선**: 관련 스킬이 있으면 반드시 활용합니다
3. **에이전트 활용**: 복잡한 작업에는 전문 에이전트를 사용합니다
4. **문서 동기화**: 코드 변경 후 `/project-docs`로 문서를 업데이트합니다
5. **서비스 경계 준수**: Vault/Forge/Quest 간 경계를 넘지 않습니다